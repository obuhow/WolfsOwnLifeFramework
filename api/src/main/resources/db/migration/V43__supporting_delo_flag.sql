-- Release 1.4 ticket 04 (И-B, Т-1, О-2): класс 1 активностей — «поддерживающие» Дела
-- (Сон, Еда, В дороге, Питание, Обед…), отличимые от Дел-единиц работы при группировке
-- и в контексте планировщика-агента. Отдельной сущности «Рутина» не вводим
-- (решение И-B): поддерживающие Дела остаются обычными Делами, Записи времени ссылаются
-- на них как на Дела.
-- О-2: новый boolean-столбец, а не переиспользование execution_mode/recurrence — у Дела
-- нет подходящего семантического поля для «класса активности», а булев флаг даёт явный,
-- индексируемый признак для выборок и сериализации в API.
ALTER TABLE delo ADD COLUMN supporting BOOLEAN NOT NULL DEFAULT FALSE;
