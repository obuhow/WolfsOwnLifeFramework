# 17 — Отчёт «Чек-лист»: история дневных чек-листов + отметки переключений

**What to build:** Страница `/reports/checklist` и endpoint `GET /api/v1/reports/checklist?from=&to=` — история по дням: пункты Чек-листа дня (тикет 16) с отметками выполнения и **Отметки переключений** из Фокус-сессий (тикет 13, Task control / review). Отчёт — это **зеркало факта, а не оценка**: считаем «отмечено N из M», показываем, куда уходили переключения, и не выводим ни процентов выполнения плана, ни стриков, ни «пропущенных дней». Экспорт в markdown/CSV — чтобы отчёт можно было положить в Заметку (тикет 04).

**Blocked by:** 04 — Заметки (кнопка «Сохранить как Заметку» пишет в модель Заметок); 13 — Режим фиксации времени (нужны отметки переключений); 16 — Чек-лист дня (нужны пункты).

**Status:** ready-for-agent

- [ ] `GET /api/v1/reports/checklist?from=YYYY-MM-DD&to=YYYY-MM-DD` (default — последние 14 дней)
- [ ] Состав ответа по дню: `{date, items: [{title, deloTitle, done, doneAt}], distractions: [{at, deloTitle|text, minutes}], checkedCount, totalCount}`
- [ ] Дни без данных присутствуют в ответе с пустыми массивами (пропуск — это факт, а не дырка)
- [ ] Агрегат периода: `checkedTotal`, `itemsTotal`, `distractionsTotal` — без процентов и без сравнения с идеалом
- [ ] `GET /api/v1/reports/checklist/export?format=md|csv` — тот же период, скачивание файла
- [ ] UI: страница «Отчёт — чек-лист» в разделе отчётов, выбор периода (7 / 14 / 30 дней / произвольный)
- [ ] UI: день = строка/карточка, отмеченные пункты — обычный серый чек, неотмеченные — обычный текст (не зачёркнуто, не красное)
- [ ] UI: переключения — отдельной строкой под пунктами дня, формулировка «переключение на X», не «отвлечение сорвало фокус»; минуты показываются только если пользователь их указал
- [ ] UI: кнопка «Сохранить как Заметку» → создаёт Заметку (тикет 04) с markdown-версией отчёта
- [ ] Копирайтинг: запрещены «провалено», «пропущено», «вы не выполнили», проценты выполнения
- [ ] API test: 3 дня с чек-листами (частично отмеченными) → корректные `checkedCount/totalCount` по дням и агрегат периода
- [ ] API test: Фокус-сессия с 2 переключениями → они видны в дне сессии
- [ ] API test: день без данных → присутствует в ответе с пустыми массивами
- [ ] API test: экспорт md возвращает файл с теми же данными
