# 12 — Статистика "волны продуктивности" (без стриков и осуждения)

**What to build:** `GET /api/v1/stats/wave?weeks=N` — фактические часы по каждой Цели за последние N ISO-недель (default 12). Дополнительно: простейший паттерн-анализ — для каждой Цели среднее, медиана, максимум, минимум часов в неделю (без оценок "хорошо/плохо"). UI: дашборд с графиком по неделям (bar chart, одна полоса на цель), подписи "медленный старт → разгон → пауза" как **норма**. Никаких красных зон, стриков, "потерянных дней", сравнений с идеалом. Нейтральный серый/синий.

**Blocked by:** 02 — Цели (нужны фактические часы по неделям).

**Status:** ready-for-agent

- [ ] `GET /api/v1/stats/wave?weeks=N` — массив {goalId, title, weeks: [{weekId, factHours}], avg, median, max, min}
- [ ] Расчёт статистик в query service (не domain entity)
- [ ] UI: страница `/stats` — bar chart по неделям для каждой цели (Recharts или Chart.js)
- [ ] Подпись под графиком: "Волнообразная продуктивность — норма. Медленный старт → разгон → пауза."
- [ ] Никаких красных цветов, стриков, сравнений с планом
- [ ] UI: переключатель количества недель (4 / 12 / 26)
- [ ] API test: данные за 12 недель → корректные avg/median/max/min
- [ ] API test: цель без фактических часов → все недели с 0, avg=0
