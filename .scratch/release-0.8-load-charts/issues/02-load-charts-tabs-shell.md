# Каркас «Диаграммы нагрузки»: замена секции «План нагрузки» на переключатель вкладок

Status: claimed
Blocked by: 01
Blocks: 03, 04, 05

## Question

Заменить две плоские таблицы секции `#load-plan` на оболочку с четырьмя вкладками и общей загрузкой данных. Тикет отвечает за каркас и общий контракт оформления; сами диаграммы приходят тикетами 03–05.

## Что сделать

1. **Новый компонент `web/src/components/LoadCharts.vue`.** Заменяет содержимое секции `#load-plan` внутри `PlanningView.vue`. Обе существующие таблицы (`capacity-list` и `curve-list`) удаляются — их данные полностью покрыты новым эндпоинтом.
2. **Якорь и маршрут не меняются.** `/roadmap#load-plan` продолжает работать и ведёт на ту же секцию. Новый маршрут не создаётся, пункт меню не добавляется, структура навигации релиза 0.5 не трогается.
3. **Заголовок секции остаётся «План нагрузки»** (термин закреплён в `CONTEXT.md`), eyebrow — «Нагрузка». «Диаграммы нагрузки» — подпись переключателя вкладок, не замена названия секции.
4. **Четыре вкладки:** Кривые · Бюджет · Лестница · Очередь. Реализуется `role="tablist"` со стрелочной навигацией и `aria-selected`. Панели вкладок 03–05 подключаются своими тикетами; в этом тикете каждая — заглушка с текстом «подключается в тикете NN».
5. **Состояние вкладки.** Активная вкладка пишется в `localStorage` (`wolf.loadCharts.tab`) и отражается в URL как query (`/roadmap?chart=curves|budget|ladder|queue`), чтобы вид передавался ссылкой. Приоритет при открытии: query → localStorage → «Кривые».
6. **Единая загрузка данных.** Один вызов `GET /api/v1/planning/load-charts` на всю секцию, результат передаётся вкладкам пропсами. Переключение вкладки не перезапрашивает данные. Состояния: загрузка, ошибка (строкой, `--wolf-ink` + нижнее правило, как текущий `.banner.error`), пусто («Нет активных проектов»).
7. **Общий контракт оформления** для всех вкладок — токены `web/src/style.css`: `--wolf-surface`, `--wolf-ink`, `--wolf-muted`, `--wolf-faint`, `--wolf-rule`, `--wolf-subrule`. Без теней, скруглений, градиентов, прогресс-баров и иконок предупреждения. Все часы и даты — `font-variant-numeric: tabular-nums`. Перебор нормы — текстом и штриховкой, **без сигнального цвета**.
8. **Общий хелпер записи** для тикетов 03–05: функция отправки изменения с дебаунсом 400 мс после завершения жеста и откатом к серверному значению при ошибке `PUT` (значение возвращается, под диаграммой появляется строка ошибки). Живёт в `LoadCharts.vue` или соседнем модуле, не дублируется в каждой вкладке.

## Testing Decisions

- Ручная приёмка: `/roadmap` — секция «План нагрузки» показывает четыре вкладки; старые таблицы отсутствуют.
- Переключение вкладок не вызывает повторный запрос `load-charts` (вкладка Network).
- Открытие `/roadmap?chart=ladder` открывает «Лестницу»; после переключения на «Очередь» и перезагрузки страницы без query открывается «Очередь».
- `/roadmap#load-plan` прокручивает к секции.
- Клавиатура: `Tab` доходит до переключателя, ←/→ меняют вкладку, `aria-selected` соответствует видимой панели.
- Тихий контракт 0.3: инспекция DOM — нет `red`/`#a33`, нет `role="alert"`, нет прогресс-баров.
- Гейт визуального тикета: сборка `web` → пересоздание контейнера → сверка отдаваемого хэша ассетов с `web/dist/assets` → аутентификация в браузере → hard refresh → инспекция DOM.

## Out of Scope

Содержимое четырёх вкладок (тикеты 03, 04, 05). Изменения в `GanttView.vue` — верхняя часть страницы Дорожная карта не трогается.

## Answer

Блокирующий тикет 01 (`GET /api/v1/planning/load-charts`, `hoursPerDelo`) уже слит в `develop`
(ветка `release-0.8/feature/01-load-charts-api` → merge-коммит `ed201f9`), поэтому его
`Status: ready-for-agent` — устаревший дрейф (см. отдельное замечание ниже). Каркас собран
поверх реального контракта эндпоинта.

**Сделано в этом тикете:**

1. Новый компонент `web/src/components/LoadCharts.vue` — заменяет содержимое секции
   `#load-plan` внутри `PlanningView.vue`. Обе старые таблицы (`capacity-list` и `curve-list`)
   удалены; их данные покрыты эндпоинтом `load-charts`.
2. Якорь и маршрут не меняются: `PlanningView.vue` монтирует `<LoadCharts/>` внутри секции
   `id="load-plan"`; `/roadmap#load-plan` по-прежнему работает (scrollBehavior роутера по
   `to.hash`). Новый маршрут не создан, пункт меню не добавлен.
3. Заголовок секции «План нагрузки» + eyebrow «Нагрузка» сохранены; «Диаграммы нагрузки» —
   подпись переключателя вкладок (`aria-label="Диаграммы нагрузки"`), не замена названия.
4. Четыре вкладки Кривые · Бюджет · Лестница · Очередь как `role="tablist"` со стрелочной
   навигацией (←/→, Home/End), `aria-selected` и `aria-controls` на панелях
   (`role="tabpanel"`). Панели 03–05 — заглушки «подключается в тикете NN».
5. Состояние вкладки: `localStorage` (`wolf.loadCharts.tab`), приоритет при открытии
   query (`?chart=...`) → localStorage → «Кривые»; вид отражается в URL через `router.replace`
   без перезагрузки данных. Подписка на `route.query.chart` ловит внешнее изменение.
6. Единая загрузка: один `GET /planning/load-charts` на секцию, результат — `ref` и передаётся
   вкладкам через `provide('loadCharts', { data, makeWriter })`. Переключение вкладки не
   перезапрашивает данные. Состояния: загрузка («Загрузка диаграмм…»), ошибка
   (`.banner.error`, `--wolf-ink` + нижнее правило, без `role="alert"`/цвета), пусто
   («Нет активных проектов» при `projects.length === 0`).
7. Общий контракт оформления — токены `style.css` (`--wolf-surface/-ink/-muted/-faint/-rule/
   -subrule`), `font-variant-numeric: tabular-nums` на панелях. Без теней/скруглений/градиентов/
   прогресс-баров/иконок предупреждения.
8. Общий хелпер записи — `web/src/loadChartsWrite.js`: `useLoadChartWrite(url, body, opts)`
   с дебаунсом 400 мс после жеста и откатом к серверному значению (`opts.onError`) при ошибке
   PUT, строкой ошибки под диаграммой. Живёт отдельным модулем, не дублируется во вкладках;
   доступен им через `inject('loadCharts').makeWriter`. (Сами PUT-вызовы добавляются в 03–05.)

**Проверка:**

- `npm run build` (web) — зелёный: `✓ 103 modules transformed`, без ошибок.
- Гейт визуального тикета (сборка web → пересоздание контейнера → сверка хэша ассетов →
  аутентификация в браузере → hard refresh → инспекция DOM) **не прогнан**: здесь нет
  поднятого стека (docker-демон недоступен стабильно на слабом хосте). Статического нарушения
  тихого контракта 0.3 в коде нет — `banner.error` использует `--wolf-ink`, `role="alert"` и
  `#a33`/`red`/`--wolf-danger` отсутствуют. Ручную приёмку из Testing Decisions (переключение
  без повторного запроса, `?chart=ladder` открывает «Лестницу», сохранение вкладки в
  localStorage, клавиатура) нужно прогнать в браузере отдельно — предлагаю после поднятия стека.

**Замечание (аудит статуса, не трогаю чужой тикет):** `01-load-charts-api.md` помечен
`ready-for-agent`, но код слит в `develop` и `LoadChartsApiIT` входит в `develop` зелёным.
Это false-open дрейф — статус должен быть `resolved`, но его меняет владелец тикета, не агент.

**Дальше:** код и статус лежат на ветке `release-0.8/feature/02-load-charts-tabs-shell`
(изолированный worktree, текущий грязный чекаут `release-0.7` не тронут). Слияние в `develop`
и прогон браузерного гейта — отдельный шаг, который сделаю по запросу.
