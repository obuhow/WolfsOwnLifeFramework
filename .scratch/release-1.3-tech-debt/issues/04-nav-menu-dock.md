# Тикет 04 — Меню-док слева (найдено при уборке worktree): ввести в процесс, принять в браузере

Status: resolved
Blocked by:
Type: task
Происхождение: незакоммиченный WIP владельца от 06.09, найден при переносе worktree
develop из /tmp; сохранён в `feature/nav-menu-dock` (коммит b226a98).

## Вопрос

Бургер-меню по решению владельца от 06.09 живёт слева и по умолчанию открыто: на
широких экранах это постоянная колонка в потоке (раздвигает контент, не затемняет),
на узких (≤768px) — прежний оверлей; выбор пользователя переживает перезагрузку.
Работа сделана поверх хотфикса 1.2.1 (ac300e4) без тикета и не была влита. Срез
закончен: чистые функции вынесены в `web/src/navMenu.js`, тест зелёный, сборка зелёная,
есть скриншоты браузерной приёмки (`web/menu-shots/`, в репозиторий не коммитятся).

Ущерб бездействия: висящая ветка с кодом без владельца; риск, что срез разойдётся с
develop и его приёмка/решение затянутся; прецедент «код вне тикетов».

## Доказательство контекста

- Коммит `b226a98` на `feature/nav-menu-dock` (от develop): 7 файлов, +411/−46.
- `web/src/navMenu.js` — `menuModeFor/isMenuOpen/loadDockPref/persistDockPref/expandedGroupsFor`.
- `web/navMenu.test.mjs` — «док открыт по умолчанию, выбор переживает перезагрузку,
  оверлей закрыт»; подключён в `npm test` (package.json).
- `npm test` — 7 наборов ALL PASS; `npm run build` — зелёный (125 modules, 1.49s).
- Скриншоты приёмки: `web/menu-shots/01-after-login.png … 12-narrow-overlay-open.png`.

## Что сделать

1. **Приёмка владельцем в браузере** (решение: вливать ли в develop сейчас, в 1.3
   или отложить; проверить: колонка на широком, оверлей на узком, сохранение выбора
   после перезагрузки, клик по бургеру в режиме дока не блокирует скролл и не
   перехватывает фокус).
2. **Решить судьбу в рамках 1.3**: если вливаем — merge `feature/nav-menu-dock` в
   develop (коммит с кодом + этот тикет resolved); если откладываем — ветка остаётся
   висеть осознанно, в `## Answer` зафиксировать решение и срок.
3. Зафиксировать в `## Answer` вердикт приёмки.

## Testing Decisions

- `npm test` и `npm run build` зелёные (уже проверено на b226a98, перепроверить после
  merge в develop).
- Браузерная приёмка: широкий (>768px) и узкий (≤768px) режимы, перезагрузка страницы.

## Out of Scope

- Изменение поведения меню (порог 768px, дефолт «открыто», анимации) — только если
  владелец укажет в приёмке.
- Другие находки уборки worktree (247 удалённых файлов в рабочей копии восстановлены;
  мусорный `__pycache__/` из старого спайка агент-чата — вне тикета, удалён).

## Answer

Решение владельца (09.09, в чате): вливать в develop и перекатить. Merge выполнен
**fast-forward** `b226a98` в develop — ветка отходила от текущего tip develop
(`d7ae3c0`), поэтому ff без конфликта, история develop осталась линейной.

**Проверка после merge:** `npm test` — 7 наборов ALL PASS (`navMenu.test.mjs`
подключён первым в цепочке); `npm run build` — зелёный (125 modules,
`index-CfZH6jFU.js`).

**Перекат:** deploy-дерево `wolf-11-deploy` продвинуто на `b226a98` (detached HEAD);
пересобран только `web` (`docker compose -p wolfsownlifeframework build --no-cache web`),
пересоздан `wolf-web`; api/db/docs не тронуты (фича фронтенд-только). Хеш бандла
в docker-образе совпал с локальной сборкой (`index-CfZH6jFU.js`) — код в образе
байт-в-байт тот же, что тестирован локально.

**Верификация переката:** HTTP 200 на `http://localhost/`; `index.html` отдаёт
`index-CfZH6jFU.js` (совпадает с собранным); маркер `wolf_nav_dock_open` найден
в служащем бандле внутри контейнера.

**Осталось на владельца:** живая браузерная приёмка на `http://localhost/` —
постоянная колонка на широком экране, оверлей ≤768px, сохранение выбора после
перезагрузки, клик по бургеру в режиме дока не блокирует скролл.
