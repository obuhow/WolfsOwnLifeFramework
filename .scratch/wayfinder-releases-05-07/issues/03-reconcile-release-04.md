# Согласование релиза 0.4 с новыми решениями

Type: task
Status: resolved
Blocked by:

## Question

Привести `.scratch/release-0.4-multiuser-demo/` в согласие с решениями этой карты, чтобы ни один открытый тикет 0.4 не противоречил тому, что будет реализовано.

Решения карты отменяют часть уже написанного 0.4. Пока это не отражено в трекере, исполнитель, взявший тикет 05 или 06, построит отменённую функциональность.

## Что сделать

**Тикет 05 — Генератор демо-фикстур**: закрыть. Единый фиксированный набор фикстур заменён тремя выбираемыми профилями. Поставить `Status: wontfix`, приписать `## Answer` с причиной и ссылкой на карту. Содержательная часть (состав фикстур, покрытие всех экранов навигации) — исходный материал для тикета о трёх профилях в 0.6, указать это явно.

**Тикет 06 — Demo Sandbox**: закрыть. Анонимный вход отменён целиком: эфемерные аккаунты, 24-часовой TTL, GC по расписанию, rate-limit по IP, `account_type=DEMO`. `Status: wontfix` + `## Answer` с причиной.

**Тикет 01 — Модель пользователя**: убрать `account_type` (`REGULAR`/`DEMO`) и `expires_at`, если они не нужны без демо-аккаунтов. Проверить: не опирается ли на `account_type` фильтрация `AgentJob` — если да, заменить условие на `status = ACTIVE`. Роль, статус, e-mail, `onboarding_completed_at`, `last_login_at` остаются.

**Тикет 08 — Мастер первого входа**: переформулировать. Три шага (Проект → Цель → норма) больше не единственный первый вход, а **ветка «Очистить профиль»** финального шага Знакомства из 0.6. Сам тикет остаётся в 0.4 и реализуется как есть — меняется только описание точки входа и снимается пункт «Демо-пользователь мастер не видит». Guard в роутере остаётся.

**Тикет 09 — Сквозная приёмка**: убрать из чек-листа пункты про демо-сессию и GC; добавить приёмку пути «регистрация по инвайту → мастер → Сегодня».

**Спецификация 0.4** (`spec.md`): править существующий файл, **не создавать параллельный**. Убрать раздел Demo Sandbox из Solution, User Stories 13–22, соответствующие Implementation Decisions и Testing Decisions (`DemoSessionApiIT`, `DemoGcIT`). Перенести отменённое в Out of Scope со ссылкой на карту. Раздел Further Notes про риск GC — снять, `UserPurgeService` остаётся только для админского удаления пользователя и по расписанию не вызывается.

**Проверить**, не ссылаются ли на 05/06 другие тикеты через `Blocked by`, и снять эти рёбра.

## Ограничения

- Существующие номера тикетов не переиспользовать, новые не заводить в 0.4 — новая функциональность живёт в 0.5–0.7.
- Закрытые 05 и 06 не удалять: они остаются историей решения, как предписывает процедура отменённых тикетов в `wolf-life-os`.
- Порядок работ 0.4 (сначала 01 и 03 как фундамент) сохраняется.
